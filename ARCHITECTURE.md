# Architecture

<!--
  Write this as you build your agent. Cover at least:

  1. How you construct valid requests from the parameter schema (query, path, header, body)
  2. How you resolve dependencies between endpoints (list → extract → substitute)
  3. How you avoid false negatives (400 from bad params vs 404 fake endpoint; retries)
  4. What is LLM-driven vs deterministic (fetch, auth, status mapping), which provider you used, and why
  5. What would break on an API you have never seen before
-->
